import { defineSoftwareMap } from "@dev.fast/progressive-review/software-map-model";

export default defineSoftwareMap({
  people: {
    operator: {
      label: "Robot operator",
      description:
        "Runs `dimos run <blueprint>` on a Go2, a desk webcam, or a recording, hands the stack a premap or a surveyed marker map, and watches the fix land in Rerun.",
    },
    developer: {
      label: "Relocalization developer",
      description:
        "Tunes the lidar aligner with tune.py studies, adds a relocalizer implementation under the base contract, and verifies the AprilTag fixtures.",
    },
  },
  systems: {
    dimos: {
      label: "DimOS",
      description:
        "The Python robotics stack: modules with typed In/Out ports, wired by autoconnect blueprints, launched by the `dimos` CLI. Relocalization places the live `world` frame inside a prior `map`; fiducial perception turns camera frames into world-frame marker poses.",
      containers: {
        relocalization: {
          label: "mapping/relocalization",
          changeStatus: "modified",
          description:
            "One base contract (load a premap, publish `world -> map` on `tf`) and its implementations: a lidar FPFH+RANSAC+ICP matcher, the Go2 variant that also merges the premap into the live map, a new fiducial matcher that places the map from one aggregated tag sighting, a recording-driven demo blueprint, and the tuning eval.",
          coverage: { globs: ["dimos/mapping/relocalization/**"] },
          components: {
            contract: {
              label: "module.py (base contract)",
              description:
                "`RelocalizationModule`: reads `map_file` (.pc2.lcm) into `self.premap`, republishes the accepted `world -> map` Transform on `tf` every 2 s and the placed premap on `loaded_map`. `submit()` takes a fix the implementation already decided to believe; `keep_relocalizing()` gates further attempts once placed (`relocalize_once`).",
              coverage: { files: ["dimos/mapping/relocalization/module.py"] },
              codeElements: {
                Config: {
                  label: "Config",
                  description: "map_file, publish_loaded_map, relocalize_once.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/module.py", fromLine: 65, toLine: 77 },
                  ],
                },
                RelocalizationModule: {
                  label: "RelocalizationModule",
                  description:
                    "Ports `tf: Out[TFMessage]`, `loaded_map: Out[PointCloud2]`; `_load_premap` stamps the cloud `map`.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/module.py", fromLine: 79, toLine: 127 },
                  ],
                },
                submit: {
                  label: "submit()",
                  description:
                    "Asserts the fix is `world -> map`, pushes it to the republish subject, marks the module placed.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/module.py", fromLine: 129, toLine: 139 },
                  ],
                },
              },
            },
            lidar: {
              label: "lidar/module.py",
              description:
                "`LidarRelocalization`: throttles `global_map` to `reloc_interval`, skips clouds under `min_local_points`, hands each to the aligner, submits the result as source `lidar`.",
              coverage: { files: ["dimos/mapping/relocalization/lidar/module.py"] },
              codeElements: {
                LidarConfig: {
                  label: "LidarConfig",
                  description: "reloc_interval 2 s, min_local_points 2000, `relocalize: RelocalizeConfig = MID360`.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/lidar/module.py", fromLine: 45, toLine: 54 },
                  ],
                },
                LidarRelocalization: {
                  label: "LidarRelocalization",
                  description: "Port `global_map: In[PointCloud2]`; builds one LidarRelocalizer at start.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/lidar/module.py", fromLine: 57, toLine: 117 },
                  ],
                },
              },
            },
            aligner: {
              label: "lidar/relocalize.py (aligner)",
              description:
                "Pure geometry, no streams: the premap is downsampled, normal-estimated and FPFH-featured once; each query runs RANSAC feature matching then wide-to-narrow point-to-plane ICP and is refused below `fitness_threshold`. Every scale is a per-rig preset.",
              coverage: { files: ["dimos/mapping/relocalization/lidar/relocalize.py"] },
              codeElements: {
                RelocalizeConfig: {
                  label: "RelocalizeConfig",
                  description: "Required per-rig scales (voxel_coarse, voxel_fine, fitness_threshold ...); no defaults on purpose.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/lidar/relocalize.py", fromLine: 63, toLine: 117 },
                  ],
                },
                MID360: {
                  label: "MID360 preset",
                  description: "Livox mid360 on a Go2, trial 229 of the go2-sf-area1 study; the only entry in PRESETS.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/lidar/relocalize.py", fromLine: 120, toLine: 141 },
                  ],
                },
                LidarRelocalizer: {
                  label: "LidarRelocalizer",
                  description: "`align()` always answers with an open3d RegistrationResult; `relocalize()` returns the inverted `world -> map` Transform or None.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/lidar/relocalize.py", fromLine: 144, toLine: 264 },
                  ],
                },
              },
            },
            go2: {
              label: "go2/module.py",
              description:
                "`Go2Relocalization`: lidar relocalization plus `merged_map` (premap transformed into `world` and carved together with the live scan) for the Go2 costmap.",
              coverage: { files: ["dimos/mapping/relocalization/go2/module.py"] },
              codeElements: {
                Go2Relocalization: {
                  label: "Go2Relocalization",
                  description: "Port `merged_map: Out[PointCloud2]`; combine_latest(global_map, world_to_map).",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/go2/module.py", fromLine: 28, toLine: 65 },
                  ],
                },
              },
            },
            fiducial: {
              label: "fiducial/module.py",
              changeStatus: "added",
              description:
                "`FiducialRelocalization`: no pointcloud premap; the surveyed tag pose is the map. Loads `marker_map_file` (.json) into `marker_id -> map_T_marker`, subscribes to the detector's `aggregated_detections`, and for a surveyed tag whose score clears `min_score` submits `world_T_map = world_T_marker @ inv(map_T_marker)` as source `fiducial`. With `relocalize_once` the first sighting of a burst places the map.",
              coverage: { files: ["dimos/mapping/relocalization/fiducial/module.py"] },
              codeElements: {
                FiducialConfig: {
                  label: "FiducialConfig",
                  description: "marker_map_file (stem or path, `.json` appended), min_score 0-1 (default 0: accept every aggregated sighting; the bar is unmeasured).",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/fiducial/module.py", fromLine: 49, toLine: 55 },
                  ],
                },
                FiducialRelocalization: {
                  label: "FiducialRelocalization",
                  description: "Port `aggregated_detections: In[Detection3DArray]`; `_fix()` reads `bbox.center` as world_T_marker and `results[0].hypothesis.score` as the health signal.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/fiducial/module.py", fromLine: 58, toLine: 132 },
                  ],
                },
              },
            },
            replay: {
              label: "blueprints.py (relocalize-mid360)",
              description:
                "`RecordingPlayer` replays a recording's lidar as a live sensor would (cloud plus the tf that places it) into a RayTracingVoxelMap, so LidarRelocalization sees the same `global_map` it would on hardware and has to find the walk inside a premap built from it.",
              coverage: { files: ["dimos/mapping/relocalization/blueprints.py"] },
              codeElements: {
                RecordingPlayer: {
                  label: "RecordingPlayer",
                  description: "Ports `lidar: Out[PointCloud2]`, `tf: Out[TFMessage]`; two SqliteStores, one pose cursor.",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/blueprints.py", fromLine: 55, toLine: 152 },
                  ],
                },
                relocalize_mid360: {
                  label: "relocalize_mid360",
                  description: "autoconnect(RecordingPlayer, RayTracingVoxelMap, LidarRelocalization(map_file=DATASET), rerun).",
                  sourceRanges: [
                    { file: "dimos/mapping/relocalization/blueprints.py", fromLine: 179, toLine: 187 },
                  ],
                },
              },
            },
            eval: {
              label: "lidar/tune.py + tune.md",
              description:
                "Probes a recording against a premap built from the same walk (ground truth = identity), reports hit rate and error, and runs the optuna study that produces a preset.",
              coverage: {
                files: [
                  "dimos/mapping/relocalization/lidar/tune.py",
                  "dimos/mapping/relocalization/lidar/tune.md",
                ],
              },
            },
            tests: {
              label: "test_module.py + fiducial/test_module.py",
              changeStatus: "modified",
              description:
                "Builds real modules and asserts the contract: submit publishes and checks frames, relocalize_once stops further attempts, port declarations. The fiducial tests construct known-truth `map_T_marker` / `world_T_marker` poses and assert `_fix` composes exactly, refuses unsurveyed tags and low scores, and takes only the first sighting of a burst.",
              coverage: {
                files: [
                  "dimos/mapping/relocalization/test_module.py",
                  "dimos/mapping/relocalization/fiducial/test_module.py",
                ],
              },
            },
          },
        },
        fiducial: {
          label: "perception/fiducial",
          changeStatus: "modified",
          description:
            "ArUco / AprilTag perception: a StreamModule that turns quality-gated camera frames plus the camera's TF pose into world-frame `Detection3DArray` messages (per frame, and now one gated, robustly aggregated pose per tag visit), the memory transformers behind it, the aggregation math, marker map file I/O, OpenCV pose helpers, a TF mirror, a desk webcam blueprint, and fixture verification for the printed boards.",
          coverage: { globs: ["dimos/perception/fiducial/**"] },
          components: {
            detector_module: {
              label: "marker_detection_stream_module.py",
              changeStatus: "modified",
              description:
                "`MarkerDetectionStreamModule`: appends each `color_image` to a stream with the `world -> optical` pose looked up in its TF buffer, runs QualityWindow, optional SpeedLimit, DetectMarkers and MarkersPerFrame, and publishes one `Detection3DArray` per processed frame on `detections`. New: the DetectMarkers output is tapped by `AggregateTagBursts`, which publishes a second port `aggregated_detections` (same message type, one detection per tag visit) so OpenCV still runs once per frame.",
              coverage: { files: ["dimos/perception/fiducial/marker_detection_stream_module.py"] },
              codeElements: {
                MarkerDetectionStreamModuleConfig: {
                  label: "MarkerDetectionStreamModuleConfig",
                  changeStatus: "modified",
                  description: "marker_length_m (required), aruco_dictionary, quality_window_s, smoothing_window, camera_info; new `aggregation: AggregationConfig` (first aggregated pose after (min_observations - 1) * quality_window_s, 1.0 s at defaults).",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_detection_stream_module.py", fromLine: 54, toLine: 70 },
                  ],
                },
                MarkerDetectionStreamModule: {
                  label: "MarkerDetectionStreamModule",
                  changeStatus: "modified",
                  description: "Ports `color_image: In[Image]`, `tf: In[TFMessage]`, `detections: Out[Detection3DArray]`, new `aggregated_detections: Out[Detection3DArray]`; start() now requires one In and two Out ports.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_detection_stream_module.py", fromLine: 73, toLine: 199 },
                  ],
                },
              },
            },
            transformers: {
              label: "marker_transformer.py",
              changeStatus: "modified",
              description:
                "Memory transformers: `DetectMarkers` emits one world-pose `Detection3DMarker` observation per decoded marker (optional sliding-window smoothing); `MarkersPerFrame` collapses that fan-out back into one `Detection3DArray` per source image, empty frames included. New `AggregateTagBursts` is a stream tap that gates each glimpse and publishes ONE aggregated pose per tag visit, edge-triggered.",
              coverage: { files: ["dimos/perception/fiducial/marker_transformer.py"] },
              codeElements: {
                DetectMarkers: {
                  label: "DetectMarkers",
                  description: "Transformer[Image, Detection3DMarker]; caches the OpenCV detector per CameraInfo.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_transformer.py", fromLine: 142, toLine: 314 },
                  ],
                },
                MarkersPerFrame: {
                  label: "MarkersPerFrame",
                  description: "Transformer[Detection3DMarker | None, Detection3DArray]; flushes on the tagged frame count.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_transformer.py", fromLine: 317, toLine: 420 },
                  ],
                },
                AggregateTagBursts: {
                  label: "AggregateTagBursts",
                  changeStatus: "added",
                  description: "Callable tap over DetectMarkers observations: feeds TagAggregator.observe(), publishes when robust_estimate() first becomes available for a marker, re-arms once the window thins under min_observations. The published detection carries the aggregated world_T_marker, transform=None, and confidence = min(1, 1/tag_noise_scale) as hypothesis.score.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_transformer.py", fromLine: 423, toLine: 477 },
                  ],
                },
              },
            },
            aggregation: {
              label: "marker_aggregation.py",
              changeStatus: "added",
              description:
                "Robust multi-sighting tag pose aggregation, OpenCV-free: per-glimpse gates (reprojection px, tag side px, view angle), a sliding-time-window `TagAggregator`, and `robust_cluster_pose` = medoid seed refined by Huber-weighted IRLS (weighted-mean translation + Markley quaternion eigen-mean). `tag_noise_scale` turns median range and reprojection error into a dimensionless variance inflation. `aggregate_by_marker_id` is the batch (survey) form; it has no caller in this change.",
              coverage: { files: ["dimos/perception/fiducial/marker_aggregation.py"] },
              codeElements: {
                AggregationConfig: {
                  label: "AggregationConfig",
                  description: "max_view_angle_deg 45, max_reproj_px 2, min_tag_px 24, min_observations 3, rotation_weight_m_per_rad 0.5, huber_delta_m 0.05, time_window_s 5.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_aggregation.py", fromLine: 44, toLine: 56 },
                  ],
                },
                TagAggregator: {
                  label: "TagAggregator",
                  description: "Streaming: `observe(TagObservation) -> rejection reason | None`, `robust_estimate(marker_id) -> TagEstimate | None` (needs min_observations in window; carries cluster-median distance_m and reproj_px).",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_aggregation.py", fromLine: 254, toLine: 288 },
                  ],
                },
                robust_cluster_pose: {
                  label: "robust_cluster_pose()",
                  description: "Medoid, then 5 Huber IRLS iterations; quaternions sign-aligned to the medoid hemisphere; Markley eigen-mean (https://ntrs.nasa.gov/citations/20070017872).",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_aggregation.py", fromLine: 191, toLine: 229 },
                  ],
                },
                tag_noise_scale: {
                  label: "tag_noise_scale()",
                  description: "(range / 0.4 m)^2 * (reproj / 1 px)^2 with floors; the detector publishes min(1, 1/scale) as the sighting's score.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_aggregation.py", fromLine: 135, toLine: 141 },
                  ],
                },
              },
            },
            marker_map: {
              label: "marker_map.py",
              changeStatus: "added",
              description:
                "Survey marker map file I/O: `load_marker_map` validates each entry into a `map_T_marker` Transform (finite translation, usable quaternion norm); `write_marker_map` emits the same JSON with `meta.marker_length_m`, and `marker_length_m_from_map` reads it back. Only `load_marker_map` has a caller in this change.",
              coverage: { files: ["dimos/perception/fiducial/marker_map.py"] },
              codeElements: {
                load_marker_map: {
                  label: "load_marker_map()",
                  description: "`marker_id -> map_T_marker` (frame_id `map`, child `marker_{id}`) from `{markers: {id: {translation, rotation}}}`.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_map.py", fromLine: 35, toLine: 63 },
                  ],
                },
                write_marker_map: {
                  label: "write_marker_map()",
                  description: "Serializes aggregated `map_T_tag` poses plus meta (schema, source_recording, marker_length_m, n_detections_aggregated). No caller at this commit.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_map.py", fromLine: 77, toLine: 100 },
                  ],
                },
              },
            },
            detect: {
              label: "marker_detect.py",
              description:
                "`detect_markers_in_image`: grayscale, ArUco detect, IPPE_SQUARE pose per marker, compose `world_T_marker = world_T_optical + optical_T_marker`, attach bbox and RMS reprojection error.",
              coverage: { files: ["dimos/perception/fiducial/marker_detect.py"] },
              codeElements: {
                detect_markers_in_image: {
                  label: "detect_markers_in_image()",
                  description: "One image in, a list of world-frame Detection3DMarker out.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_detect.py", fromLine: 39, toLine: 140 },
                  ],
                },
              },
            },
            pose: {
              label: "marker_pose.py",
              description:
                "OpenCV helpers: CameraInfo to K/D, fisheye undistortion, solvePnP IPPE_SQUARE, Rodrigues to Transform, ArUco detector construction, corner bbox and reprojection error.",
              coverage: { files: ["dimos/perception/fiducial/marker_pose.py"] },
              codeElements: {
                estimate_marker_pose: {
                  label: "estimate_marker_pose()",
                  description: "(rvec, tvec) for optical <- marker; fisheye corners are undistorted into pinhole K first.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_pose.py", fromLine: 74, toLine: 113 },
                  ],
                },
                create_aruco_detector: {
                  label: "create_aruco_detector()",
                  description: "cv2.aruco.ArucoDetector for a named predefined dictionary.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_pose.py", fromLine: 139, toLine: 150 },
                  ],
                },
              },
            },
            tf_module: {
              label: "marker_tf_module.py",
              description:
                "`MarkerTfModule`: mirrors each detection's bbox center pose into TF as `world -> markers` (identity) and `markers -> marker_{id}`; owns no pose estimation. Still fed by the per-frame `detections` port only.",
              coverage: { files: ["dimos/perception/fiducial/marker_tf_module.py"] },
              codeElements: {
                MarkerTfModule: {
                  label: "MarkerTfModule",
                  description: "Ports `detections: In[Detection3DArray]`, `tf: Out[TFMessage]`.",
                  sourceRanges: [
                    { file: "dimos/perception/fiducial/marker_tf_module.py", fromLine: 59, toLine: 151 },
                  ],
                },
              },
            },
            desk_blueprint: {
              label: "blueprints/desk_marker_tf.py",
              description:
                "`desk-marker-tf`: a webcam CameraModule, a static `world -> base_link -> camera_optical` TF republisher, the detector at 5 cm tags, and the TF mirror under the `marker_tf` namespace.",
              coverage: {
                files: [
                  "dimos/perception/fiducial/blueprints/desk_marker_tf.py",
                  "dimos/perception/fiducial/blueprints/fixtures/camera_info.yaml",
                ],
              },
            },
            fixture_verification: {
              label: "fixture_verification.py",
              description:
                "Checks the checked-in AprilTag board photos against the generated board layout: detected ids, apparent scale, footprint, layout reprojection error.",
              coverage: { files: ["dimos/perception/fiducial/fixture_verification.py"] },
            },
            tests: {
              label: "test_*.py",
              changeStatus: "modified",
              description:
                "Hermetic tests with a synthetic 640x480 CameraInfo: pose math, the transformers, the stream module's ports and pipeline (now asserting outputs {detections, aggregated_detections}), the TF mirror, the desk blueprint, fixture verification. No tests for marker_aggregation.py or marker_map.py at this commit.",
              coverage: {
                globs: ["dimos/perception/fiducial/test_*.py"],
                files: ["dimos/perception/fiducial/blueprints/test_desk_marker_tf.py"],
              },
            },
          },
        },
        go2_stack: {
          label: "robot/unitree/go2",
          changeStatus: "modified",
          description:
            "The Unitree Go2 robot: the WebRTC connection module that publishes lidar, camera and odometry, and the smart blueprints that compose mapping, navigation, markers and relocalization on top of it.",
          coverage: { globs: ["dimos/robot/unitree/go2/**"] },
          components: {
            connection: {
              label: "connection.py (GO2Connection)",
              description:
                "Ports `lidar`, `pointcloud`, `odom`, `color_image`, `camera_info`, `tf` out, `cmd_vel` in; `camera_info_static` carries the front camera intrinsics blueprints hand to the marker detector.",
              coverage: { files: ["dimos/robot/unitree/go2/connection.py"] },
            },
            smart_blueprints: {
              label: "blueprints/smart/unitree_go2.py",
              changeStatus: "modified",
              description:
                "`unitree_go2` (basic + VoxelGridMapper + CostMapper + planner + explorer + patrolling + MovementManager) and the stacks built on it: `unitree_go2_markers`, `unitree_go2_relocalization`, new `unitree_go2_fiducial_relocalization`, `unitree_go2_memory`.",
              coverage: { files: ["dimos/robot/unitree/go2/blueprints/smart/unitree_go2.py"] },
              codeElements: {
                unitree_go2: {
                  label: "unitree_go2",
                  sourceRanges: [
                    { file: "dimos/robot/unitree/go2/blueprints/smart/unitree_go2.py", fromLine: 43, toLine: 51 },
                  ],
                },
                unitree_go2_markers: {
                  label: "unitree_go2_markers",
                  description: "MarkerDetectionStreamModule(marker_length_m=0.1, camera_info_static) + MarkerTfModule; `detections` over LCM.",
                  sourceRanges: [
                    { file: "dimos/robot/unitree/go2/blueprints/smart/unitree_go2.py", fromLine: 80, toLine: 98 },
                  ],
                },
                unitree_go2_relocalization: {
                  label: "unitree_go2_relocalization",
                  description: "unitree_go2 + Go2Relocalization.blueprint().",
                  sourceRanges: [
                    { file: "dimos/robot/unitree/go2/blueprints/smart/unitree_go2.py", fromLine: 100, toLine: 102 },
                  ],
                },
                unitree_go2_fiducial_relocalization: {
                  label: "unitree_go2_fiducial_relocalization",
                  changeStatus: "added",
                  description: "unitree_go2_markers + FiducialRelocalization.blueprint(); tag sightings only, no pointcloud premap. The marker map comes from `-o fiducialrelocalization.marker_map_file=<survey>`; n_workers=12.",
                  sourceRanges: [
                    { file: "dimos/robot/unitree/go2/blueprints/smart/unitree_go2.py", fromLine: 105, toLine: 108 },
                  ],
                },
              },
            },
          },
        },
        registry: {
          label: "robot/all_blueprints.py",
          changeStatus: "modified",
          description:
            "Generated name -> dotted-path tables for every blueprint and module the CLI can run (`relocalize-mid360`, `unitree-go2-relocalization`, `desk-marker-tf`, `lidar-relocalization`, `marker-tf-module`, ...). New entries: `unitree-go2-fiducial-relocalization` and `fiducial-relocalization`.",
          coverage: { files: ["dimos/robot/all_blueprints.py"] },
        },
        core: {
          label: "core",
          description:
            "`Module` with typed `In`/`Out` ports and `@rpc` lifecycle, the TF buffer every module carries, `autoconnect` blueprints that wire ports by name and type, transports (LCM, zenoh) and the global config.",
          coverage: { globs: ["dimos/core/**"] },
        },
        memory: {
          label: "memory",
          description:
            "`StreamModule`, `Stream`, `Transformer` and observation stores (SqliteStore recordings, NullStore); the QualityWindow / SpeedLimit transforms, `Stream.tap`, and the Recorder.",
          coverage: { globs: ["dimos/memory/**"] },
        },
        mapping: {
          label: "mapping (other)",
          description:
            "Live mapping around relocalization: the VoxelGridMapper that turns lidar into `global_map`, the native RayTracingVoxelMap, the CostMapper that consumes `global_map` or `merged_map`, loop closure and PGO.",
          coverage: {
            globs: [
              "dimos/mapping/*",
              "dimos/mapping/voxels/**",
              "dimos/mapping/ray_tracing/**",
              "dimos/mapping/loop_closure/**",
              "dimos/mapping/pointclouds/**",
              "dimos/mapping/occupancy/**",
              "dimos/mapping/utils/**",
              "dimos/mapping/cli/**",
            ],
          },
          components: {
            voxel_mapper: {
              label: "voxels/module.py (VoxelGridMapper)",
              description: "StreamModule: `lidar: In[PointCloud2]` accumulated into a VoxelGrid, `global_map: Out[PointCloud2]` every `emit_every` frames.",
              coverage: { files: ["dimos/mapping/voxels/module.py"] },
            },
            ray_tracing: {
              label: "ray_tracing/module.py (RayTracingVoxelMap)",
              description: "Native module: `lidar` + `tf` in, `global_map`, `local_map` out; places sensor-frame clouds itself.",
              coverage: { files: ["dimos/mapping/ray_tracing/module.py"] },
            },
            costmapper: {
              label: "costmapper.py (CostMapper)",
              description: "`global_map` and `merged_map: In[PointCloud2]` to `global_costmap: Out[OccupancyGrid]`.",
              coverage: { files: ["dimos/mapping/costmapper.py"] },
            },
          },
        },
        perception: {
          label: "perception (other)",
          description:
            "Detection types shared with fiducial: `Detection3DMarker` (marker id, corners, dictionary, reprojection error) and `ImageDetections3D.to_ros_detection3d_array`; object detection and perception memory.",
          coverage: { globs: ["dimos/perception/detection/**", "dimos/perception/memory/**", "dimos/perception/experimental/**"] },
        },
        msgs: {
          label: "msgs",
          description:
            "LCM-encodable message types: PointCloud2, Transform / TFMessage, Image, CameraInfo, Detection3D / Detection3DArray, OccupancyGrid.",
          coverage: { globs: ["dimos/msgs/**"] },
        },
        cli: {
          label: "cli",
          description:
            "The `dimos` Typer app: `run <blueprint>`, `map`, `topic`, `data`, and `apriltag` (prints tag boards; its `_grid_layout` is what fixture verification reproduces).",
          coverage: { globs: ["dimos/cli/**"] },
        },
        hardware: {
          label: "hardware",
          description: "Sensor and actuator drivers: CameraModule / Webcam, lidar (FAST-LIO, mid360), drive trains, manipulators.",
          coverage: { globs: ["dimos/hardware/**"] },
        },
        navigation: {
          label: "navigation",
          description: "Costmap consumers: ReplanningAStarPlanner, WavefrontFrontierExplorer, PatrollingModule, MovementManager, nav_3d.",
          coverage: { globs: ["dimos/navigation/**"] },
        },
        visualization: {
          label: "visualization",
          description: "`vis_module`: Rerun / web viewer bridge with per-entity visual overrides.",
          coverage: { globs: ["dimos/visualization/**"] },
        },
        utils: {
          label: "utils",
          description: "`resolve_named_path` (stem -> data file, LFS-fetched on miss), structlog `setup_logger`, reactive backpressure helpers.",
          coverage: { globs: ["dimos/utils/**"] },
        },
        other: {
          label: "other packages",
          description: "Agents, skills, control, teleop, simulation, web, protocol, models, manipulation and the rest of the stack; untouched by this change.",
          coverage: {
            globs: [
              "dimos/agents/**",
              "dimos/skills/**",
              "dimos/control/**",
              "dimos/teleop/**",
              "dimos/simulation/**",
              "dimos/web/**",
              "dimos/protocol/**",
              "dimos/models/**",
              "dimos/manipulation/**",
              "dimos/imitation/**",
              "dimos/stream/**",
              "dimos/types/**",
              "dimos/spec/**",
              "dimos/porcelain/**",
              "dimos/cloud/**",
              "dimos/evals/**",
              "dimos/experimental/**",
            ],
          },
        },
        native: {
          label: "native (Rust / C++)",
          description: "Native modules (ray tracing, Point-LIO, mid360 driver) built through the Cargo workspace.",
          coverage: { globs: ["native/**"], files: ["Cargo.toml"] },
        },
      },
      dataStores: {
        premaps: {
          label: "Premap files (.pc2.lcm)",
          kind: "fileStore",
          description:
            "Prior maps: one LCM-encoded PointCloud2 per stem, e.g. `recording_go2_mid360_2026-05-29_4-45pm-PST_corrected.pc2.lcm`, resolved by `resolve_named_path` and LFS-fetched on miss. The premap *is* the `map` frame.",
        },
        marker_maps: {
          label: "Marker map files (.json)",
          kind: "fileStore",
          changeStatus: "added",
          description:
            "Surveyed tag poses: `{meta: {schema: map_T_tag, source_recording, marker_length_m, n_detections_aggregated}, markers: {id: {translation: [x, y, z], rotation: [x, y, z, w]}}}`, one `map_T_marker` per id, resolved by `resolve_named_path(marker_map_file, '.json')`. No writer ships in this change.",
        },
        recordings: {
          label: "Recordings (.db)",
          kind: "database",
          description:
            "SqliteStore recordings of a walk: per-stream observation rows (`fastlio_lidar` clouds with pose and ts) that RecordingPlayer replays and tune.py probes.",
        },
      },
    },
    go2_robot: {
      label: "Unitree Go2",
      external: true,
      description: "The quadruped itself: WebRTC stream of lidar scans, front camera frames and odometry; takes `cmd_vel`.",
    },
    open3d: {
      label: "Open3D",
      external: true,
      description: "Registration pipeline: voxel downsampling, normal estimation, FPFH features, RANSAC feature matching, point-to-plane ICP. Imported lazily.",
    },
    opencv: {
      label: "OpenCV",
      external: true,
      description: "cv2.aruco ArucoDetector, solvePnP (IPPE_SQUARE), fisheye undistortPoints, Rodrigues, projectPoints.",
    },
    lcm: {
      label: "LCM bus",
      external: true,
      description: "UDP multicast pub/sub carrying module ports between processes; `/marker_detection/detections` is pinned to it explicitly.",
    },
    rerun: {
      label: "Rerun",
      external: true,
      description: "The 3D viewer: `world/global_map`, `world/loaded_map` (appears only once a fix lands), `world/merged_map`.",
    },
  },
  relationships: [
    { kind: "semantic", from: "operator", to: "dimos.cli", label: "dimos run <blueprint>" },
    { kind: "semantic", from: "operator", to: "dimos.go2_stack.smart_blueprints", label: "unitree-go2-relocalization / unitree-go2-markers / unitree-go2-fiducial-relocalization" },
    { kind: "semantic", from: "operator", to: "dimos.relocalization.replay", label: "relocalize-mid360 on a recording" },
    { kind: "semantic", from: "operator", to: "dimos.fiducial.desk_blueprint", label: "desk-marker-tf on a webcam" },
    { kind: "semantic", from: "operator", to: "dimos.premaps", label: "Supplies map_file" },
    { kind: "semantic", from: "operator", to: "dimos.marker_maps", label: "Supplies -o fiducialrelocalization.marker_map_file" },
    { kind: "semantic", from: "developer", to: "dimos.relocalization.eval", label: "Runs tune.py probes and studies" },
    { kind: "semantic", from: "developer", to: "dimos.fiducial.fixture_verification", label: "Verifies the printed AprilTag boards" },
    { kind: "semantic", from: "dimos.cli", to: "dimos.registry", label: "Resolves the blueprint name" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.go2_stack.smart_blueprints", label: "unitree-go2-* (now also unitree-go2-fiducial-relocalization)" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.relocalization.replay", label: "relocalize-mid360" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.relocalization.fiducial.FiducialRelocalization", label: "fiducial-relocalization" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.fiducial.desk_blueprint", label: "desk-marker-tf" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.other", label: "Names modules across every package" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.hardware", label: "Names sensor modules" },
    { kind: "semantic", from: "dimos.registry", to: "dimos.navigation", label: "Names navigation modules" },
    { kind: "semantic", from: "dimos.cli", to: "dimos.native", label: "Builds and launches native modules" },
    { kind: "semantic", from: "go2_robot", to: "dimos.go2_stack.connection", label: "WebRTC: lidar, color_image, odom" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2", to: "dimos.go2_stack.connection", label: "unitree_go2_basic = GO2Connection + vis" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2", to: "dimos.mapping.voxel_mapper", label: "VoxelGridMapper(emit_every=5)" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2", to: "dimos.mapping.costmapper", label: "CostMapper" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2", to: "dimos.navigation", label: "Planner, explorer, patrolling, MovementManager" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_relocalization", to: "dimos.relocalization.go2.Go2Relocalization", label: "Go2Relocalization.blueprint()" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_markers", to: "dimos.fiducial.detector_module.MarkerDetectionStreamModuleConfig", label: "marker_length_m=0.1, camera_info=camera_info_static" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_markers", to: "dimos.fiducial.tf_module.MarkerTfModule", label: "MarkerTfModule.blueprint()" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_markers", to: "lcm", label: "LCMTransport('/marker_detection/detections')" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_fiducial_relocalization", to: "dimos.go2_stack.smart_blueprints.unitree_go2_markers", label: "autoconnect(unitree_go2_markers, ...)" },
    { kind: "semantic", from: "dimos.go2_stack.smart_blueprints.unitree_go2_fiducial_relocalization", to: "dimos.relocalization.fiducial.FiducialRelocalization", label: "FiducialRelocalization.blueprint() (marker_map_file via -o)" },
    { kind: "semantic", from: "dimos.go2_stack.connection", to: "dimos.mapping.voxel_mapper", label: "lidar: Out[PointCloud2] (world frame)" },
    { kind: "semantic", from: "dimos.go2_stack.connection", to: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", label: "color_image: Out[Image], tf: Out[TFMessage]" },
    { kind: "semantic", from: "dimos.mapping.voxel_mapper", to: "dimos.relocalization.lidar.LidarRelocalization", label: "global_map: Out[PointCloud2] -> In" },
    { kind: "semantic", from: "dimos.mapping.voxel_mapper", to: "dimos.memory", label: "StreamModule + VoxelMapTransformer" },
    { kind: "semantic", from: "dimos.relocalization.contract.RelocalizationModule", to: "dimos.premaps", label: "_load_premap: PointCloud2.lcm_decode(map_file + .pc2.lcm)" },
    { kind: "semantic", from: "dimos.relocalization.contract.RelocalizationModule", to: "dimos.core", label: "tf: Out[TFMessage] world -> map every 2 s; loaded_map: Out[PointCloud2]" },
    { kind: "semantic", from: "dimos.relocalization.contract.RelocalizationModule", to: "dimos.msgs", label: "Transform, TFMessage, PointCloud2" },
    { kind: "semantic", from: "dimos.relocalization.contract.RelocalizationModule", to: "dimos.utils", label: "resolve_named_path, setup_logger" },
    { kind: "semantic", from: "dimos.relocalization.lidar.LidarConfig", to: "dimos.relocalization.contract.Config", label: "extends" },
    { kind: "semantic", from: "dimos.relocalization.lidar.LidarConfig", to: "dimos.relocalization.aligner.RelocalizeConfig", label: "relocalize: RelocalizeConfig = MID360" },
    { kind: "semantic", from: "dimos.relocalization.lidar.LidarRelocalization", to: "dimos.relocalization.contract.RelocalizationModule", label: "extends; gates on keep_relocalizing()" },
    { kind: "semantic", from: "dimos.relocalization.lidar.LidarRelocalization", to: "dimos.relocalization.contract.submit", label: "submit(tf, 'lidar')" },
    { kind: "semantic", from: "dimos.relocalization.lidar.LidarRelocalization", to: "dimos.relocalization.aligner.LidarRelocalizer", label: "relocalize(local cloud) per throttled global_map" },
    { kind: "semantic", from: "dimos.relocalization.aligner.LidarRelocalizer", to: "open3d", label: "FPFH + RANSAC, then point-to-plane ICP" },
    { kind: "semantic", from: "dimos.relocalization.aligner.LidarRelocalizer", to: "dimos.relocalization.aligner.RelocalizeConfig", label: "Prepares the premap under one config" },
    { kind: "semantic", from: "dimos.relocalization.aligner.MID360", to: "dimos.relocalization.aligner.RelocalizeConfig", label: "instance" },
    { kind: "semantic", from: "dimos.relocalization.eval", to: "dimos.relocalization.aligner.MID360", label: "A study freezes its winner here" },
    { kind: "semantic", from: "dimos.relocalization.eval", to: "dimos.relocalization.aligner.LidarRelocalizer", label: "align() on accumulated scans, scored against identity" },
    { kind: "semantic", from: "dimos.relocalization.eval", to: "dimos.recordings", label: "Reads the walk and its premap" },
    { kind: "semantic", from: "dimos.relocalization.go2.Go2Relocalization", to: "dimos.relocalization.lidar.LidarRelocalization", label: "extends" },
    { kind: "semantic", from: "dimos.relocalization.go2.Go2Relocalization", to: "dimos.mapping.costmapper", label: "merged_map: Out[PointCloud2] (premap in world + live scan)" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialConfig", to: "dimos.relocalization.contract.Config", label: "extends: marker_map_file, min_score" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialRelocalization", to: "dimos.relocalization.contract.RelocalizationModule", label: "extends; gates each sighting on keep_relocalizing()" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialRelocalization", to: "dimos.relocalization.contract.submit", label: "submit(world_T_marker @ inv(map_T_marker), 'fiducial')" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialRelocalization", to: "dimos.marker_maps", label: "resolve_named_path(marker_map_file, '.json') at start" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialRelocalization", to: "dimos.fiducial.marker_map.load_marker_map", label: "marker_id -> map_T_marker.to_matrix()" },
    { kind: "semantic", from: "dimos.relocalization.fiducial.FiducialRelocalization", to: "dimos.fiducial.aggregation", label: "matrix_from_pose7(bbox.center) = world_T_marker" },
    { kind: "semantic", from: "dimos.relocalization.replay.RecordingPlayer", to: "dimos.recordings", label: "SqliteStore.replay(stream=fastlio_lidar)" },
    { kind: "semantic", from: "dimos.relocalization.replay.RecordingPlayer", to: "dimos.mapping.ray_tracing", label: "lidar + tf: Out -> RayTracingVoxelMap" },
    { kind: "semantic", from: "dimos.mapping.ray_tracing", to: "dimos.relocalization.lidar.LidarRelocalization", label: "global_map: Out[PointCloud2] (replay)" },
    { kind: "semantic", from: "dimos.relocalization.replay.relocalize_mid360", to: "dimos.relocalization.lidar.LidarRelocalization", label: "LidarRelocalization.blueprint(map_file=DATASET, publish_loaded_map=True)" },
    { kind: "semantic", from: "dimos.relocalization.replay.relocalize_mid360", to: "dimos.visualization", label: "vis_module('rerun', loaded_map at 1.5 mm)" },
    { kind: "semantic", from: "dimos.visualization", to: "rerun", label: "Logs entities" },
    { kind: "semantic", from: "dimos.relocalization.tests", to: "dimos.relocalization.contract.submit", label: "Asserts frame check and relocalize_once" },
    { kind: "semantic", from: "dimos.relocalization.tests", to: "dimos.relocalization.fiducial.FiducialRelocalization", label: "_fix composes exactly; refuses unsurveyed / low score; first sighting only" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.memory", label: "StreamModule; QualityWindow, SpeedLimit, Stream.tap, stream_to_port" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.fiducial.transformers.DetectMarkers", label: "pipeline: DetectMarkers(emit_empty_frames=True)" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.fiducial.transformers.AggregateTagBursts", label: "markers.tap(AggregateTagBursts(aggregated_detections.publish, config.aggregation))" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.fiducial.transformers.MarkersPerFrame", label: "then MarkersPerFrame(frame_id=world_frame)" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.fiducial.tf_module.MarkerTfModule", label: "detections: Out[Detection3DArray] -> In" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.relocalization.fiducial.FiducialRelocalization", label: "aggregated_detections: Out[Detection3DArray] -> In (one per tag visit)" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", to: "dimos.fiducial.pose", label: "camera_optical_frame_id, is_fisheye_model" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModuleConfig", to: "dimos.fiducial.transformers.DetectMarkers", label: "marker_length_m, aruco_dictionary, smoothing_window" },
    { kind: "semantic", from: "dimos.fiducial.detector_module.MarkerDetectionStreamModuleConfig", to: "dimos.fiducial.aggregation.AggregationConfig", label: "aggregation: AggregationConfig = Field(default_factory=AggregationConfig)" },
    { kind: "semantic", from: "dimos.fiducial.transformers.DetectMarkers", to: "dimos.fiducial.detect.detect_markers_in_image", label: "per observation, with the cached detector and K/D" },
    { kind: "semantic", from: "dimos.fiducial.transformers.DetectMarkers", to: "dimos.fiducial.pose.create_aruco_detector", label: "rebuilds the detector when CameraInfo changes" },
    { kind: "semantic", from: "dimos.fiducial.transformers.MarkersPerFrame", to: "dimos.perception", label: "ImageDetections3D.to_ros_detection3d_array" },
    { kind: "semantic", from: "dimos.fiducial.transformers.AggregateTagBursts", to: "dimos.fiducial.aggregation.TagAggregator", label: "observe(tag_observation(det)) per glimpse; robust_estimate(marker_id)" },
    { kind: "semantic", from: "dimos.fiducial.transformers.AggregateTagBursts", to: "dimos.fiducial.aggregation.tag_noise_scale", label: "confidence = min(1, 1/scale) -> hypothesis.score" },
    { kind: "semantic", from: "dimos.fiducial.transformers.AggregateTagBursts", to: "dimos.perception", label: "ImageDetections3D([aggregated]).to_ros_detection3d_array" },
    { kind: "semantic", from: "dimos.fiducial.aggregation.TagAggregator", to: "dimos.fiducial.aggregation.robust_cluster_pose", label: "over the in-window glimpses of one marker" },
    { kind: "semantic", from: "dimos.fiducial.aggregation.TagAggregator", to: "dimos.fiducial.aggregation.AggregationConfig", label: "gate_reason thresholds, time_window_s, min_observations" },
    { kind: "semantic", from: "dimos.fiducial.aggregation.robust_cluster_pose", to: "dimos.fiducial.aggregation.AggregationConfig", label: "rotation_weight_m_per_rad, huber_delta_m" },
    { kind: "semantic", from: "dimos.fiducial.marker_map.write_marker_map", to: "dimos.marker_maps", label: "Emits the JSON load_marker_map reads (no caller yet)" },
    { kind: "semantic", from: "dimos.fiducial.marker_map.load_marker_map", to: "dimos.marker_maps", label: "Parses and validates {markers: {id: {translation, rotation}}}" },
    { kind: "semantic", from: "dimos.fiducial.marker_map.load_marker_map", to: "dimos.msgs", label: "Transform(frame_id='map', child_frame_id='marker_{id}')" },
    { kind: "semantic", from: "dimos.fiducial.detect.detect_markers_in_image", to: "dimos.fiducial.pose.estimate_marker_pose", label: "per marker corner set" },
    { kind: "semantic", from: "dimos.fiducial.detect.detect_markers_in_image", to: "dimos.fiducial.pose", label: "rvec_tvec_to_transform, marker_corners_to_bbox, marker_reprojection_error" },
    { kind: "semantic", from: "dimos.fiducial.detect.detect_markers_in_image", to: "dimos.perception", label: "emits Detection3DMarker" },
    { kind: "semantic", from: "dimos.fiducial.pose.estimate_marker_pose", to: "opencv", label: "cv2.solvePnP(SOLVEPNP_IPPE_SQUARE), fisheye.undistortPoints" },
    { kind: "semantic", from: "dimos.fiducial.pose.create_aruco_detector", to: "opencv", label: "cv2.aruco.ArucoDetector" },
    { kind: "semantic", from: "dimos.fiducial.tf_module.MarkerTfModule", to: "dimos.core", label: "tf: Out[TFMessage] world -> markers -> marker_{id}" },
    { kind: "semantic", from: "dimos.fiducial.tf_module.MarkerTfModule", to: "dimos.msgs", label: "Detection3DArray in, Transform / TFMessage out" },
    { kind: "semantic", from: "dimos.fiducial.desk_blueprint", to: "dimos.fiducial.detector_module.MarkerDetectionStreamModuleConfig", label: "marker_length_m=0.05, desk CameraInfo" },
    { kind: "semantic", from: "dimos.fiducial.desk_blueprint", to: "dimos.fiducial.tf_module.MarkerTfModule", label: "marker_namespace_prefix='marker_tf'" },
    { kind: "semantic", from: "dimos.fiducial.desk_blueprint", to: "dimos.hardware", label: "CameraModule(hardware=Webcam)" },
    { kind: "semantic", from: "dimos.fiducial.desk_blueprint", to: "lcm", label: "LCMTransport('/marker_detection/detections')" },
    { kind: "semantic", from: "dimos.fiducial.fixture_verification", to: "dimos.cli", label: "dimos.cli.apriltag._grid_layout" },
    { kind: "semantic", from: "dimos.fiducial.fixture_verification", to: "opencv", label: "Detects tags in the fixture photos" },
    { kind: "semantic", from: "dimos.fiducial.tests", to: "dimos.fiducial.detector_module.MarkerDetectionStreamModule", label: "Asserts ports {color_image, tf} -> {detections, aggregated_detections}" },
    { kind: "semantic", from: "dimos.fiducial.tests", to: "dimos.fiducial.transformers.DetectMarkers", label: "Synthetic marker frames" },
    { kind: "semantic", from: "dimos.core", to: "lcm", label: "LCMTransport, rpc backend" },
    { kind: "semantic", from: "dimos.core", to: "dimos.msgs", label: "Port payloads" },
  ],
});
